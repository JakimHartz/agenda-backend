# Agenda de Contactos - API Backend

Este es el backend de la aplicación de Agenda de Contactos, desarrollado con **Spring Boot 4.1.0** y **Java 21**.

## Tecnologías utilizadas
*   **Java 21** & **Spring Boot**
*   **Spring Data JPA** (Persistencia)
*   **PostgreSQL** (Base de datos relacional)
*   **Lombok** & **Spring Validation**

## Requisitos previos
*   JDK 21 instalado.
*   Servidor PostgreSQL corriendo localmente.

## Instalación y ejecución
1. Crea una base de datos en PostgreSQL llamada `agenda_db`.
2. Configura tus credenciales de base de datos en `src/main/resources/application.properties`.
3. Ejecuta la aplicación desde tu IDE o mediante terminal:
   ```bash
   ./mvnw spring-boot:run

---

# Contact Agenda - Backend API

This is the backend of the Contact Agenda application, developed with **Spring Boot 4.1.0** and **Java 21**.

## Technologies Used

* **Java 21** & **Spring Boot**
* **Spring Data JPA** (Persistence)
* **PostgreSQL** (Relational Database)
* **Lombok** & **Spring Validation**

## Prerequisites

* JDK 21 installed.
* A local PostgreSQL server running.

## Installation and Setup

1. Create a PostgreSQL database named `agenda_db`.
2. Configure your database credentials in `src/main/resources/application.properties`.
3. Run the application from your IDE or using the terminal:

   ```bash
   ./mvnw spring-boot:run
   ```

---

# Agenda de Contacts - API Backend

Il s'agit du backend de l'application **Agenda de Contacts**, développé avec **Spring Boot 4.1.0** et **Java 21**.

## Technologies utilisées

* **Java 21** et **Spring Boot**
* **Spring Data JPA** (Persistance)
* **PostgreSQL** (Base de données relationnelle)
* **Lombok** et **Spring Validation**

## Prérequis

* JDK 21 installé.
* Un serveur PostgreSQL en cours d'exécution en local.

## Installation et exécution

1. Créez une base de données PostgreSQL nommée `agenda_db`.
2. Configurez vos identifiants de base de données dans `src/main/resources/application.properties`.
3. Exécutez l'application depuis votre IDE ou à l'aide du terminal :

   ```bash
   ./mvnw spring-boot:run
   ```
